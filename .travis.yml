language: cpp
services:
  - docker
before_script:
  - docker pull registry.gitlab.com/s.arlyapov/build_cpp/broker
  - docker run -d registry.gitlab.com/s.arlyapov/build_cpp/broker bash
script:
  - cmake .
  - cmake --build .
