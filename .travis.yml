dist: bionic
language: cpp
before_script:
  - sudo -i
  - apt-get update
  - apt-get install -y libpoco-dev uuid-dev pkg-config unixodbc-dev libgtest-dev build-essential libssl-dev git wget curl unzip autoconf
  - 
  - cd /usr/src/gtest
  - cmake CMakeLists.txt
  - make install
  - cd /
  - git clone https://github.com/protocolbuffers/protobuf.git
  - cd /protobuf
  - git checkout v3.3.0
  - git submodule update --init --recursive
  - ./autogen.sh
  - ./configure
  - make install
  - apt-get install -y protobuf-compiler
script:
  - cmake .
  - cmake --build .
