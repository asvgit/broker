dist: bionic
language: cpp
before_script:
  - sudo apt install -y cmake build-essential
  - sudo apt install -y libpoco-dev uuid-dev pkg-config protobuf-compiler unixodbc-dev libgtest-dev libprotobuf-dev
script:
  - cmake .
  - cmake --build .
